function orderAjax(opt) {
  // Dummy
  opt.callback({success: true });
}

function shipAjax(opt) {
  // Dummy
  opt.callback({success: true });
}

function retrieveAjax(opt) {
  // Dummy
  opt.callback({success: true });
}